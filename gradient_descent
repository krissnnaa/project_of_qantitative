%gradient descent p(k)=-delf(x(k))
x=[1 ;2];
del=0.0001;
alph=1;
N= length(x);
for k=1:N
    p(k)=-g(x(k));
    alph(k)= del*alph(k);
    x(k+1)= x(k)+alpha(k)* p(k);
    %f(x+k)=f(x)+ p*g(x(k)) + (1/2) * (p(k))'*h(k)*p(k);
    
    
    